📘 ReactJS Documentation Notes
1. ReactJS Introduction

ReactJS হলো Facebook (Meta) দ্বারা তৈরি করা একটি JavaScript লাইব্রেরি।

মূল কাজ → UI (User Interface) বানানো।

বৈশিষ্ট্য:

Component Based Architecture

Declarative (UI কেমন হবে সেটা বলে দাও, React সেটা বানায়)

Virtual DOM ব্যবহার করে fast rendering

Reusable Components

2. Core Concepts
a) Components

Components = ছোট ছোট reusable UI অংশ।

দুই ধরনের Component:

Functional Component (modern, hooks ব্যবহার করে)

Class Component (পুরনো style, এখন কম ব্যবহার হয়)

b) JSX (JavaScript XML)

JavaScript + HTML মিশ্র syntax।

Example:

const element = <h1>Hello React!</h1>;

c) Props

Props = Properties

Component এ data পাঠানোর উপায়।

Example:

function Welcome(props) {
  return <h1>Hello, {props.name}</h1>;
}

d) State

Component এর ভেতরের data যেটা পরিবর্তন হতে পারে।

Functional Component এ → useState hook ব্যবহার করা হয়।

import { useState } from "react";

function Counter() {
  const [count, setCount] = useState(0);

  return (
    <div>
      <p>{count}</p>
      <button onClick={() => setCount(count + 1)}>Increase</button>
    </div>
  );
}

e) Events

React এ events camelCase হয়: onClick, onChange ইত্যাদি।

<button onClick={handleClick}>Click Me</button>

3. React Hooks
a) useState

State ম্যানেজ করার জন্য।

b) useEffect

Side effects (API call, timer, DOM update) করার জন্য।

c) useContext

Props drilling ছাড়াই data শেয়ার করার জন্য।

d) useRef

DOM element বা mutable value ধরার জন্য।

e) useReducer

Complex state management এর জন্য।

4. React Lifecycle (Class Component এ বেশি ব্যবহৃত)

Mounting → componentDidMount

Updating → componentDidUpdate

Unmounting → componentWillUnmount

(Functional components এ এগুলো useEffect দিয়ে handle করা হয়)

5. React Router

Single Page Application (SPA) বানাতে ব্যবহৃত।

পেজ পরিবর্তন না করে URL পরিবর্তন হয়।

Example:

import { BrowserRouter, Routes, Route, Link } from "react-router-dom";

function App() {
  return (
    <BrowserRouter>
      <nav>
        <Link to="/">Home</Link>
        <Link to="/about">About</Link>
      </nav>
      <Routes>
        <Route path="/" element={<Home />} />
        <Route path="/about" element={<About />} />
      </Routes>
    </BrowserRouter>
  );
}

6. React with API

Fetch API বা Axios দিয়ে ডেটা আনা হয়।

সাধারণত useEffect এর ভেতর ব্যবহার করা হয়।

import { useEffect, useState } from "react";

function Users() {
  const [users, setUsers] = useState([]);

  useEffect(() => {
    fetch("https://jsonplaceholder.typicode.com/users")
      .then(res => res.json())
      .then(data => setUsers(data));
  }, []);

  return (
    <ul>
      {users.map(user => <li key={user.id}>{user.name}</li>)}
    </ul>
  );
}

7. Advanced Topics

Context API → global state management।

Redux / Zustand / Recoil → বড় প্রোজেক্টে state management।

React Query / SWR → API data cache + sync।

Custom Hooks → নিজের hook বানিয়ে reuse করা যায়।

8. React Best Practices

✅ Components ছোট রাখা
✅ Meaningful নাম দেওয়া
✅ Reusable component বানানো
✅ Folder structure ঠিক রাখা
✅ PropTypes / TypeScript দিয়ে type-checking

9. React Project Structure (Basic)
my-app/
  ├── public/
  ├── src/
  │   ├── components/
  │   │   └── Navbar.jsx
  │   ├── pages/
  │   │   └── Home.jsx
  │   ├── App.jsx
  │   ├── index.jsx
  │   └── styles/
  │       └── App.css
  ├── package.json
